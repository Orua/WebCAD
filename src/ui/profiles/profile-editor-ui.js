import {validateProfile} from '../../modeling/profiles/profile-model.js';
import {profileIntersections,editProfileEndpoint,filletProfileLines,trimProfileCircle} from '../../modeling/profiles/profile-editing.js';
import {mountProfileCanvas} from './profile-canvas-ui.js';
import {PROFILE_PRIMITIVES,primitiveEntities} from '../../modeling/profiles/profile-primitives.js';

const number = (label, value, field) => {
  const input = document.createElement('input');
  input.type = 'number'; input.step = 'any'; input.required = true;
  input.setAttribute('aria-label', label); input.dataset.field = field; input.value = String(value);
  return input;
};
const select = (label, values, value) => {
  const control = document.createElement('select'); control.setAttribute('aria-label', label);
  for (const [key, text] of values) { const option = document.createElement('option'); option.value = key; option.textContent = text; control.append(option); }
  control.value = value; return control;
};
const seeded = (type, start=[0,0], end=[10,0]) => ({id:crypto.randomUUID(),type,...(type==='circle'?{centerMm:[0,0],diameterMm:20}:type==='arc3'?{startMm:start,midMm:[(start[0]+end[0])/2,(start[1]+end[1])/2+5],endMm:end}:{startMm:start,endMm:end})});
export function mountProfileEditor(form, initial) {
  const controls=document.createElement('div');controls.className='profile-editor';
  const toolbar=document.createElement('div');toolbar.className='profile-editor-toolbar';
  const rows=document.createElement('div');rows.className='profile-entity-rows';
  const precise=document.createElement('section');precise.className='profile-selection-properties';const preciseTitle=document.createElement('strong');preciseTitle.textContent='选中图元尺寸';const entityButtons=document.createElement('div');entityButtons.className='profile-editor-toolbar';precise.append(preciseTitle,entityButtons,rows);
  const drawingGroups=new Map([['outer','外框 1']]);let activeRole='outer';
  const activeGroup=select('当前绘制轮廓',[['outer','外框 1']],'outer');activeGroup.hidden=true;
  const status=document.createElement('p');status.className='property-footnote';status.setAttribute('aria-live','polite');
  const output=select('轮廓输出',[['face','闭合面'],['wire','开放/闭合线框']],initial?.output||'face');
  const extraHoleRoles=new Set();for(let index=0;index<(initial?.regions?.length||0);index++)for(let hole=1;hole<initial.regions[index].holeLoopIds.length;hole++)extraHoleRoles.add(`hole-${index+1}-${hole+1}`);
  const extraPathRoles=[...(initial?.loops||[]),...(initial?.chains||[])].filter(path=>!initial?.regions?.some(region=>region.outerLoopId===path.id||region.holeLoopIds.includes(path.id))).map(path=>[`path-${path.id}`,`线框路径 ${path.id}`]);
  const addButton=(label,handler)=>{const button=document.createElement('button');button.type='button';button.textContent=label;button.addEventListener('click',()=>{try{undo.push(read({validate:false}));handler();refreshEntities();}catch(error){info.textContent=error.message;}});toolbar.append(button);};
  const field=(parent,label,value,key)=>{const wrapper=document.createElement('label');wrapper.append(document.createTextNode(label),number(label,value,key));parent.append(wrapper);};
  function refreshGroups(){for(const control of [activeGroup,...rows.querySelectorAll('.profile-role')]){const previous=control===activeGroup?activeRole:control.value;control.replaceChildren();for(const [key,label] of drawingGroups){const option=document.createElement('option');option.value=key;option.textContent=label;control.append(option);}if(control!==activeGroup){const option=document.createElement('option');option.value='construction';option.textContent='辅助线';control.append(option);}control.value=previous;control.hidden=drawingGroups.size===1&&previous!=='construction';}}
  function ensureGroup(role){if(role==='construction'||drawingGroups.has(role))return;const parts=role.split('-');drawingGroups.set(role,role==='outer'?'外框 1':role.startsWith('outer-')?'外框 '+parts[1]:role.startsWith('hole-')?'外框 '+parts[1]+' 的孔 '+(parts[2]||'1'):'路径 '+(drawingGroups.size+1));}
  function addRow(entity,role=activeRole,reversed=false){
    ensureGroup(role);
    if(rows.children.length>=500)throw new Error('轮廓最多 500 个实体');
    const row=document.createElement('div');row.className='profile-entity-row';row.dataset.id=entity.id;row.dataset.type=entity.type;row.dataset.reversed=String(reversed);if(entity.projectionSource)row.dataset.projectionSource=JSON.stringify(entity.projectionSource);
    const title=document.createElement('strong');title.textContent={line:'直线',arc3:'三点圆弧',circle:'圆',rectangle:'矩形',roundedRectangle:'圆角矩形',capsule:'长圆'}[entity.type];
    const roleChoices=[...drawingGroups,['construction','辅助线']];
    const roleInput=select('所属轮廓',roleChoices,role);roleInput.className='profile-role';row.append(title,roleInput);
    if(PROFILE_PRIMITIVES.includes(entity.type)){field(row,'起始 X',entity.originMm[0],'ox');field(row,'起始 Y',entity.originMm[1],'oy');field(row,entity.type==='capsule'?'总长':'宽度',entity.widthMm,'width');field(row,entity.type==='capsule'?'宽度':'高度',entity.heightMm,'height');if(entity.type==='roundedRectangle')field(row,'角 R',entity.cornerRadiusMm,'cornerRadius');const convert=document.createElement('button');convert.type='button';convert.textContent='转为独立直线 / 圆弧';convert.addEventListener('click',()=>{try{const current=read({validate:false}),source=current.entities.find(item=>item.id===row.dataset.id);undo.push(current);const parts=primitiveEntities(source),reverse=row.dataset.reversed==='true';for(const part of reverse?[...parts].reverse():parts){addRow(part,row.querySelector('.profile-role').value,reverse);rows.insertBefore(rows.lastElementChild,row);}row.remove();form.dispatchEvent(new Event('input',{bubbles:true}));refreshEntities();}catch(error){info.textContent=error.message;}});row.append(convert);}
    else if(entity.type==='circle') { field(row,'圆心 X',entity.centerMm[0],'cx');field(row,'圆心 Y',entity.centerMm[1],'cy');field(row,'直径 Ø',entity.diameterMm,'diameter'); }
    else { field(row,'起点 X',entity.startMm[0],'sx');field(row,'起点 Y',entity.startMm[1],'sy');if(entity.type==='arc3'){field(row,'经过 X',entity.midMm[0],'mx');field(row,'经过 Y',entity.midMm[1],'my');}field(row,'终点 X',entity.endMm[0],'ex');field(row,'终点 Y',entity.endMm[1],'ey'); }
    if(entity.type==='line'){const dx=entity.endMm[0]-entity.startMm[0],dy=entity.endMm[1]-entity.startMm[1];field(row,'一次设置长度',Math.hypot(dx,dy),'setLength');field(row,'一次设置角度 °',Math.atan2(dy,dx)*180/Math.PI,'setAngle');const applyDimensions=document.createElement('button');applyDimensions.type='button';applyDimensions.textContent='按长度 / 角度设置终点';applyDimensions.addEventListener('click',()=>{const length=Number(row.querySelector('[data-field="setLength"]').value),angle=Number(row.querySelector('[data-field="setAngle"]').value)*Math.PI/180;if(!Number.isFinite(length)||length<=0||!Number.isFinite(angle)){info.textContent='长度须大于零，角度须为有限数字';return;}undo.push(read({validate:false}));const x=Number(row.querySelector('[data-field="sx"]').value),y=Number(row.querySelector('[data-field="sy"]').value);row.querySelector('[data-field="ex"]').value=String(x+length*Math.cos(angle));row.querySelector('[data-field="ey"]').value=String(y+length*Math.sin(angle));form.dispatchEvent(new Event('input',{bubbles:true}));});row.append(applyDimensions);}
    const remove=document.createElement('button');remove.type='button';remove.textContent='删除段';remove.setAttribute('aria-label',`删除 ${title.textContent}`);remove.addEventListener('click',()=>{undo.push(read({validate:false}));row.remove();form.dispatchEvent(new Event('input',{bubbles:true}));});row.append(remove);rows.append(row);
    form.dispatchEvent(new Event('input',{bubbles:true}));
  }
  function nextOuter(){const regions=[...drawingGroups.keys()].filter(role=>role==='outer'||role.startsWith('outer-')).map(role=>role==='outer'?1:Number(role.split('-')[1])),region=Math.max(0,...regions)+1;if(region>16)throw new Error('最多 16 个外框；按实际需要添加');activeRole=region===1?'outer':`outer-${region}`;ensureGroup(activeRole);return activeRole;}
  const groupRows=()=>[...rows.children].filter(row=>row.querySelector('.profile-role').value===activeRole);
  const groupClosed=()=>{const members=groupRows(),entities=read({validate:false}).entities.filter(entity=>members.some(row=>row.dataset.id===entity.id));if(!entities.length)return false;if(entities.length===1&&(PROFILE_PRIMITIVES.includes(entities[0].type)||entities[0].type==='circle'))return true;const ends=entities.map(entity=>{if(!entity.startMm)return null;return members.find(row=>row.dataset.id===entity.id).dataset.reversed==='true'?[entity.endMm,entity.startMm]:[entity.startMm,entity.endMm];});return ends.every(Boolean)&&ends.every((pair,index)=>Math.hypot(pair[1][0]-ends[(index+1)%ends.length][0][0],pair[1][1]-ends[(index+1)%ends.length][0][1])<1e-6);};
  function preset(entities){for(const row of groupRows())row.remove();entities.forEach(item=>addRow(item));source.value=rows.lastElementChild.dataset.id;canvas.finish();canvas.fit();}
  addButton('矩形',()=>preset([{id:crypto.randomUUID(),type:'rectangle',originMm:[0,0],widthMm:40,heightMm:30}]));
  addButton('圆角矩形',()=>preset([{id:crypto.randomUUID(),type:'roundedRectangle',originMm:[0,0],widthMm:40,heightMm:30,cornerRadiusMm:3}]));
  addButton('长圆',()=>preset([{id:crypto.randomUUID(),type:'capsule',originMm:[0,0],widthMm:40,heightMm:20}]));
  addButton('圆',()=>preset([seeded('circle')]));
  addButton('清空',()=>{rows.replaceChildren();drawingGroups.clear();activeRole='outer';ensureGroup(activeRole);canvas.finish();form.dispatchEvent(new Event('input',{bubbles:true}));});
  addButton('新增外框',()=>{nextOuter();canvas.finish();});
  addButton('闭合轮廓',()=>{if(groupClosed()){canvas.finish();return;}const current=read({validate:false}),members=groupRows(),entities=members.map(row=>current.entities.find(entity=>entity.id===row.dataset.id)),point=(entity,row,start)=>row.dataset.reversed==='true'?(start?entity.endMm:entity.startMm):(start?entity.startMm:entity.endMm);if(entities.length<2||entities.some(entity=>!entity.startMm))throw new Error('先连续绘制两条或更多直线 / 圆弧，再闭合');const start=point(entities[0],members[0],true),end=point(entities.at(-1),members.at(-1),false);addRow({id:crypto.randomUUID(),type:'line',startMm:end,endMm:start});canvas.finish();});
  addButton('添加孔',()=>{const region=activeRole==='outer'?'1':activeRole.match(/^(?:outer|hole)-(\d+)/)?.[1];if(!region)throw new Error('先选择孔所属的外框');const outer=region==='1'?'outer':`outer-${region}`;if(![...rows.children].some(row=>row.querySelector('.profile-role').value===outer))throw new Error('先绘制外框，再添加内部孔');const prefix=`hole-${region}`,existing=[...drawingGroups.keys()].filter(role=>role===prefix||role.startsWith(prefix+'-'));if(drawingGroups.size>=50)throw new Error('轮廓环最多 50 个');activeRole=existing.length?`${prefix}-${existing.length+1}`:prefix;extraHoleRoles.add(activeRole);ensureGroup(activeRole);canvas.finish();info.textContent=`正在绘制外框 ${region} 的孔 ${existing.length+1}；在外框内部画圆或闭合路径。`;});
  const outputButtons=[];output.hidden=true;
  for(const [value,label] of [['face','闭合面'],['wire','线框']]){const button=document.createElement('button');button.type='button';button.textContent=label;button.setAttribute('aria-label','输出'+label);button.addEventListener('click',()=>{output.value=value;for(const item of outputButtons)item.setAttribute('aria-pressed',String(item.dataset.output===value));form.dispatchEvent(new Event('input',{bubbles:true}));});button.dataset.output=value;button.setAttribute('aria-pressed',String(value===output.value));outputButtons.push(button);toolbar.append(button);}
  activeGroup.addEventListener('change',()=>{activeRole=activeGroup.value;canvas.finish();refreshEntities();});toolbar.append(activeGroup);
  controls.append(toolbar,output,status,precise);form.append(controls);
  const initialRoles=new Map(),originalLoopIds=new Map(),originalRegionIds=new Map(),originalKinds=new Map();
  const orderedEntities=params=>{const order=[...(params.loops||[]),...(params.chains||[])].flatMap(path=>path.edges.map(ref=>ref.entityId)),byId=new Map(params.entities.map(entity=>[entity.id,entity]));return [...new Set([...order,...byId.keys()])].map(id=>byId.get(id)).filter(Boolean);};
  if(initial){for(const loop of initial.loops||[]){const regionIndex=initial.regions?.findIndex(region=>region.outerLoopId===loop.id||region.holeLoopIds?.includes(loop.id))??-1;const region=initial.regions?.[regionIndex];const role=regionIndex<0?'path-'+loop.id:loop.id===region.outerLoopId?(regionIndex===0?'outer':`outer-${regionIndex+1}`):`hole-${regionIndex+1}${region.holeLoopIds.indexOf(loop.id)>0?'-'+(region.holeLoopIds.indexOf(loop.id)+1):''}`;originalLoopIds.set(role,loop.id);originalKinds.set(role,'loop');for(const entry of loop.edges)initialRoles.set(entry.entityId,role);}for(const chain of initial.chains||[]){const role='path-'+chain.id;originalLoopIds.set(role,chain.id);originalKinds.set(role,'chain');for(const entry of chain.edges)initialRoles.set(entry.entityId,role);}for(const entity of orderedEntities(initial))addRow(entity,initialRoles.get(entity.id)||'construction',[...(initial.loops||[]),...(initial.chains||[])].flatMap(path=>path.edges).find(ref=>ref.entityId===entity.id)?.reversed||false);}
  else addRow({id:crypto.randomUUID(),type:'rectangle',originMm:[0,0],widthMm:40,heightMm:30});
  const read=({validate=true}={})=>{
    const entities=[],directions=new Map(),groups={};
    for(const row of rows.children){const value=key=>{const input=row.querySelector(`[data-field="${key}"]`),n=Number(input?.value);if(!input?.value.trim()||!Number.isFinite(n))throw new Error(`${key} 需要有限数值`);return n;};
      const entity={id:row.dataset.id,type:row.dataset.type};if(row.dataset.projectionSource)entity.projectionSource=JSON.parse(row.dataset.projectionSource);
      if(PROFILE_PRIMITIVES.includes(entity.type)){entity.originMm=[value('ox'),value('oy')];entity.widthMm=value('width');entity.heightMm=value('height');if(entity.type==='roundedRectangle')entity.cornerRadiusMm=value('cornerRadius');}
      else if(entity.type==='circle'){entity.centerMm=[value('cx'),value('cy')];entity.diameterMm=value('diameter');}
      else {entity.startMm=[value('sx'),value('sy')];entity.endMm=[value('ex'),value('ey')];if(entity.type==='arc3')entity.midMm=[value('mx'),value('my')];}
      const role=row.querySelector('.profile-role').value;directions.set(entity.id,row.dataset.reversed==='true');if(role==='construction')entity.construction=true;else (groups[role]??=[]).push(entity.id);
      entities.push(entity);
    }
    const loops=[],chains=[],regions=[];
    if(output.value==='face'){
      for(const [role,ids] of Object.entries(groups))if(ids.length)loops.push({id:originalLoopIds.get(role)||role,edges:ids.map(entityId=>({entityId,reversed:directions.get(entityId)}))});
      for(let i=1;i<=16;i++){const outer=i===1?'outer':`outer-${i}`,hole=`hole-${i}`,holes=Object.keys(groups).filter(role=>role===hole||role.startsWith(hole+'-'));if(groups[outer]?.length)regions.push({id:initial?.regions?.[i-1]?.id||`region-${i}`,outerLoopId:originalLoopIds.get(outer)||outer,holeLoopIds:holes.map(role=>originalLoopIds.get(role)||role)});else if(holes.length)throw new Error(`区域 ${i} 有孔环但没有外环`);}
    }else for(const [role,ids] of Object.entries(groups))if(ids.length){const path={id:originalLoopIds.get(role)||`chain-${role}`,edges:ids.map(entityId=>({entityId,reversed:directions.get(entityId)}))};(originalKinds.get(role)==='loop'?loops:chains).push(path);}
    const params={profileVersion:1,entities,loops,chains,regions,output:output.value};if(validate)validateProfile(params);return params;
  };
  const editDetails=document.createElement('section');editDetails.className='profile-curve-edit';const editTitle=document.createElement('strong');editTitle.textContent='轮廓编辑';editDetails.append(editTitle);let editActive=false,pickingCurve='source';
  const editPanel=document.createElement('div');editPanel.className='profile-edit-panel';editDetails.append(editPanel);
  const mode=select('轮廓编辑方式',[['trim','修剪到交点'],['trimCircle','整圆修剪为圆弧'],['extend','沿原曲线延伸'],['fillet','两直线二维圆角']],'trim');
  const source=select('源曲线',[],''),target=select('目标曲线',[],''),candidate=select('选定交点',[],''),end=select('编辑端点',[['end','末端'],['start','起端']],'end');
  const radius=number('二维圆角 R mm',2,'radius');
  const candidateEnd=select('整圆修剪终交点',[],''),keepSide=select('保留圆弧方向',[['ccw','保留逆时针区间'],['cw','保留顺时针区间']],'ccw');
  const info=document.createElement('p');info.setAttribute('aria-live','polite');
  const editButtons=document.createElement('div');editButtons.className='profile-editor-toolbar';mode.hidden=source.hidden=target.hidden=true;
  const editModeButtons=[];for(const [value,label] of [['trim','修剪'],['extend','延伸'],['fillet','二维圆角'],['trimCircle','整圆裁剪']]){const button=document.createElement('button');button.type='button';button.textContent=label;button.dataset.editMode=value;button.setAttribute('aria-pressed','false');button.addEventListener('click',()=>{editActive=true;mode.value=value;for(const control of editModeButtons)control.setAttribute('aria-pressed',String(control.dataset.editMode===value));refreshCandidates();});editButtons.append(button);editModeButtons.push(button);}
  const selectedCurves=document.createElement('p');selectedCurves.className='property-footnote';
  for(const [value,label] of [['source','选源曲线'],['target','选目标曲线']]){const button=document.createElement('button');button.type='button';button.textContent=label;button.addEventListener('click',()=>{pickingCurve=value;info.textContent=value==='source'?'在绘图区点击要编辑的曲线。':'在绘图区点击与源曲线相交的目标曲线。';});editButtons.append(button);}
  const undo=[];
  const roleMap=params=>{const map=new Map();for(const loop of params.loops||[]){const index=params.regions?.findIndex(region=>region.outerLoopId===loop.id||region.holeLoopIds?.includes(loop.id))??-1;const region=params.regions?.[index];const role=[...originalLoopIds].find(([,id])=>id===loop.id)?.[0]||(index<0?'path-'+loop.id:loop.id===region.outerLoopId?(index===0?'outer':`outer-${index+1}`):`hole-${index+1}${region.holeLoopIds.indexOf(loop.id)>0?'-'+(region.holeLoopIds.indexOf(loop.id)+1):''}`);for(const item of loop.edges)map.set(item.entityId,role);}for(const chain of params.chains||[]){const role=[...originalLoopIds].find(([,id])=>id===chain.id)?.[0]||(chain.id.startsWith('chain-')?chain.id.slice(6):'path-'+chain.id);for(const item of chain.edges)map.set(item.entityId,role);}return map;};
  const load=params=>{const roles=roleMap(params);rows.replaceChildren();output.value=params.output;for(const item of orderedEntities(params))addRow(item,roles.get(item.id)||'construction',[...(params.loops||[]),...(params.chains||[])].flatMap(path=>path.edges).find(ref=>ref.entityId===item.id)?.reversed||false);refreshEntities();};
  const refreshEntities=()=>{refreshGroups();const current=read({validate:false});for(const [control,previous] of [[source,source.value],[target,target.value]]){control.replaceChildren();for(const [index,item] of current.entities.entries()){const option=document.createElement('option');option.value=item.id;option.textContent=`${index+1}. ${{line:'直线',arc3:'圆弧',circle:'圆',rectangle:'矩形',roundedRectangle:'圆角矩形',capsule:'长圆'}[item.type]}`;control.append(option);}if(previous&&current.entities.some(item=>item.id===previous))control.value=previous;else if(control===target&&control.options.length>1)control.selectedIndex=1;}for(const item of outputButtons)item.setAttribute('aria-pressed',String(item.dataset.output===output.value));status.textContent=`${current.entities.length} 个图元 · ${drawingGroups.get(activeRole)||'当前路径'}${groupClosed()?'已闭合':'：点回起点，或点击「闭合轮廓」'}`;entityButtons.replaceChildren();for(const [index,item] of current.entities.entries()){const row=rows.querySelector(`[data-id="${item.id}"]`),selected=item.id===source.value;row.hidden=!selected;row.style.display=selected?'':'none';const button=document.createElement('button');button.type='button';button.textContent=`${index+1}. ${row.querySelector('strong').textContent}`;button.setAttribute('aria-pressed',String(selected));button.addEventListener('click',()=>{source.value=item.id;const role=row.querySelector('.profile-role').value;if(role!=='construction')activeRole=role;refreshEntities();});entityButtons.append(button);}precise.hidden=!current.entities.length;refreshCandidates();canvas?.render();undoButton.disabled=!undo.length;};
  const refreshCandidates=()=>{candidate.replaceChildren();candidateEnd.replaceChildren();candidateEnd.hidden=keepSide.hidden=true;radius.hidden=end.hidden=candidate.hidden=true;apply.hidden=!editActive;selectedCurves.textContent=`源：${source.selectedOptions[0]?.textContent||'未选择'} · 目标：${target.selectedOptions[0]?.textContent||'未选择'}`;if(!editActive){info.textContent='';return;}radius.hidden=mode.value!=='fillet';end.hidden=['fillet','trimCircle'].includes(mode.value);keepSide.hidden=mode.value!=='trimCircle';try{const current=read({validate:false}),a=current.entities.find(e=>e.id===source.value),b=current.entities.find(e=>e.id===target.value);if([a,b].some(entity=>entity&&PROFILE_PRIMITIVES.includes(entity.type))){info.textContent='参数图形先用「转为独立直线 / 圆弧」，再点击需要编辑的两条曲线。';return;}if(a&&b&&a.id!==b.id&&mode.value!=='fillet')for(const item of profileIntersections(a,b)){for(const control of [candidate,candidateEnd]){const option=document.createElement('option');option.value=item.id;option.textContent=`${item.id}: ${item.point.map(n=>Number(n.toFixed(4))).join(', ')} mm`;control.append(option);}}candidate.hidden=mode.value==='fillet'||candidate.options.length<2;candidateEnd.hidden=mode.value!=='trimCircle'||candidateEnd.options.length<3;if(candidateEnd.options.length>1)candidateEnd.selectedIndex=1;info.textContent=mode.value==='fillet'?'选两条相邻直线，输入圆角半径。':mode.value==='trimCircle'?'选择两个交点和要保留的方向。':candidate.length?`${candidate.length} 个交点；选择起端或末端后应用。`:'先点击源曲线，再点击「选目标曲线」或按 Shift 点击目标。';}catch(error){info.textContent=error.message;}};
  const proposedEdit=()=>{const current=read(),next=mode.value==='fillet'?filletProfileLines(current,{firstId:source.value,secondId:target.value,radiusMm:Number(radius.value),arcId:crypto.randomUUID()}):mode.value==='trimCircle'?trimProfileCircle(current,{entityId:source.value,targetId:target.value,startCandidateId:candidate.value,endCandidateId:candidateEnd.value,keepSide:keepSide.value}):editProfileEndpoint(current,{mode:mode.value,entityId:source.value,targetId:target.value,candidateId:candidate.value,end:end.value});return {current,next};};
  const apply=document.createElement('button');apply.type='button';apply.textContent='应用到轮廓草稿';apply.addEventListener('click',()=>{try{const {current,next}=proposedEdit();undo.push(current);load(next);info.textContent='已修改轮廓草稿；完成轮廓前可局部撤销。';}catch(error){info.textContent=error.message;}});
  const undoButton=document.createElement('button');undoButton.type='button';undoButton.disabled=true;undoButton.textContent='撤销绘制';undoButton.addEventListener('click',()=>{if(undo.length)load(undo.pop());});
  for(const control of [mode,source,target,end,radius])control.addEventListener('change',refreshCandidates);
  const editHint=document.createElement('p');editHint.className='property-footnote';editHint.textContent='点击图元修改尺寸；修剪或延伸时点击源曲线，按住 Shift 点击目标曲线。';
  editPanel.append(editButtons,editHint,selectedCurves,mode,source,target,candidate,candidateEnd,keepSide,end,radius,apply);controls.append(editDetails,undoButton,info);
  let canvas;canvas=mountProfileCanvas(controls,{read:()=>read({validate:false}),onAdd:entity=>{undo.push(read({validate:false}));if(groupClosed()||['circle','rectangle'].includes(entity.type)&&groupRows().length)nextOuter();addRow(entity);refreshEntities();return groupClosed();},onSelect:(id,event)=>{if(event.shiftKey||pickingCurve==='target'){target.value=id;pickingCurve='source';}else{source.value=id;const row=rows.querySelector(`[data-id="${id}"]`);if(row){const role=row.querySelector('.profile-role').value;if(role!=='construction')activeRole=role;}}for(const row of rows.children)row.classList.toggle('selected',row.dataset.id===source.value||editActive&&row.dataset.id===target.value);refreshEntities();},onError:error=>{info.textContent=error.message;}});controls.insertBefore(canvas.root,precise);
  apply.addEventListener('pointerenter',()=>{try{const {current,next}=proposedEdit();canvas.showEditPreview(current,next);}catch(error){info.textContent=error.message;}});apply.addEventListener('pointerleave',()=>canvas.clearEditPreview());apply.addEventListener('focus',()=>{try{const {current,next}=proposedEdit();canvas.showEditPreview(current,next);}catch(error){info.textContent=error.message;}});apply.addEventListener('blur',()=>canvas.clearEditPreview());
  form.addEventListener('input',()=>{try{canvas.render();refreshEntities();}catch(error){info.textContent=error.message;}});form.addEventListener('change',()=>{try{refreshEntities();}catch(error){info.textContent=error.message;}});
  refreshEntities();
  return {read,root:controls,appendProjected(entities){for(const entity of entities)addRow(entity,'construction');refreshEntities();}};
}
